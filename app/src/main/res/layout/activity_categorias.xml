<?xml version="1.0" encoding="utf-8"?>
<!--
    TELA 7 – Categorias
    Lista todas as categorias disponíveis em um RecyclerView.
    Cada item mostra emoji, nome, descrição e quantidade de notas.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fundo_tela">

    <!-- Toolbar com título e botão de voltar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_categorias"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:background="@color/roxo_primario"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationContentDescription="Voltar"
        app:title="Categorias"
        app:titleTextColor="#FFFFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- RecyclerView com a lista de categorias (item_categoria.xml) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_categorias"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingHorizontal="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_categorias"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
